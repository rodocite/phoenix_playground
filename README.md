# React + Node + Phoenix Integration WIP
Overview:
  * Views - React
  * API Adapter and Client Server - Node
  * API - Phoenix

To start your app:
  * Install dependencies with `mix deps.get` then `npm install`
  * Create and migrate your database with `mix ecto.create && mix ecto.migrate`
  * Start the stack with `npm start`
